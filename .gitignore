# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Databases — the demo DuckDB is large (~500 MB) and reproducible.
# Do NOT commit it directly. Either build it (scripts/build_demo_db.py),
# track a slimmed copy via Git LFS, or host it and set LGA_AMP_DB_URL.
*.duckdb
*.duckdb.wal
*.wal

# Generated demo report
data/*.html

# Streamlit secrets — never commit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db
